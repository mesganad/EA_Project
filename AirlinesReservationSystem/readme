Steps to run the program:

1. run application.java of registry
2. run application.java of the airlines-reservation-system (server)
3. run application.java of client

To run the server:
login by either of the following accounts:
using postMan => POST: localhost:8090/login:
admin: {"username":"aron",
        "password":"12345"
        }
        or
        passenger: {"username":"samuel",
                "password":"12345"
                }
                or
                agent: {"username":"ambasa",
                                "password":"12345"
                                }
  Then, do any requests using the endpoints and the token generated.